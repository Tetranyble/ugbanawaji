import Link from "next/link";
import { Search } from "lucide-react";
import { searchPublicContent } from "@/lib/platform-data";
import { Card, CardContent } from "@/components/ui/card";
import { Badge } from "@/components/ui/badge";
import { SearchForm } from "@/components/site/search-form";
export const dynamic="force-dynamic";
export default async function SearchPage({searchParams}:{searchParams:Promise<{q?:string}>}){const {q=""}=await searchParams;let results=[] as Awaited<ReturnType<typeof searchPublicContent>>;try{results=await searchPublicContent(q)}catch{}return <main className="section-space"><div className="container-shell max-w-5xl"><p className="section-kicker">Search</p><h1 className="mt-3 text-4xl font-extrabold">Search the engineering portfolio.</h1><p className="mt-3 text-muted-foreground">Find case studies, articles, ADRs, engineering principles, open-source work and notes.</p><div className="mt-7"><SearchForm initialQuery={q}/></div>{q?<div className="mt-8"><p className="text-sm text-muted-foreground">{results.length} result{results.length===1?"":"s"} for <strong className="text-foreground">“{q}”</strong></p><div className="mt-5 space-y-3">{results.length?results.map((r,i)=><Card key={`${r.url}-${i}`}><CardContent className="p-5"><div className="flex items-start justify-between gap-4"><div><Badge>{r.label}</Badge><h2 className="mt-3 text-xl font-bold"><Link href={r.url} className="hover:text-primary">{r.title}</Link></h2><p className="mt-2 line-clamp-2 text-sm leading-6 text-muted-foreground">{r.summary}</p></div><Search className="mt-1 size-4 shrink-0 text-primary"/></div></CardContent></Card>):<Card className="border-dashed"><CardContent className="p-8"><h2 className="text-xl font-bold">Nothing matched that search.</h2><p className="mt-2 text-muted-foreground">Try a broader engineering term such as Java, fintech, reliability, architecture or AI.</p></CardContent></Card>}</div></div>:null}</div></main>}
