import Link from "next/link";
import { notFound } from "next/navigation";
import { getSeriesWithPosts } from "@/lib/platform-data";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { Badge } from "@/components/ui/badge";
export const dynamic="force-dynamic";
export default async function SeriesPage({params}:{params:Promise<{slug:string}>}){const{slug}=await params;let data=null;try{data=await getSeriesWithPosts(slug)}catch{}if(!data)notFound();return <main className="section-space"><div className="container-shell max-w-5xl"><p className="section-kicker">Article series</p><h1 className="section-title mt-3">{data.series.title}</h1>{data.series.description?<p className="mt-4 max-w-3xl text-lg leading-8 text-muted-foreground">{data.series.description}</p>:null}<div className="mt-10 space-y-4">{data.posts.map((post,index)=><Card key={post.id}><CardContent className="grid gap-4 p-5 sm:grid-cols-[60px_1fr]"><div className="grid size-12 place-items-center rounded-xl bg-muted font-extrabold text-primary">{index+1}</div><div><div className="flex flex-wrap items-center gap-2"><Badge>{post.contentType.replaceAll("_"," ")}</Badge><span className="text-xs text-muted-foreground">{post.readingMinutes} min read</span></div><h2 className="mt-2 text-xl font-bold"><Link href={`/blog/${post.slug}`} className="hover:text-primary">{post.title}</Link></h2><p className="mt-2 text-sm text-muted-foreground">{post.excerpt}</p></div></CardContent></Card>)}{!data.posts.length?<p className="text-muted-foreground">This series has no public articles yet.</p>:null}</div></div></main>}
