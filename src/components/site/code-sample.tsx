"use client";
import { useState } from "react";
import { Check, Copy } from "lucide-react";
import { Button } from "@/components/ui/button";
export function CodeSample({title,language,code,explanation,copyLabel,copiedLabel}:{title:string;language:string;code:string;explanation?:string;copyLabel?:string;copiedLabel?:string}){const[copied,setCopied]=useState(false);return <div className="overflow-hidden rounded-2xl border border-border bg-[#0b1020] text-slate-200"><div className="flex items-center justify-between gap-4 border-b border-white/10 px-4 py-3"><div><p className="font-semibold text-white">{title}</p><p className="text-xs uppercase tracking-[.1em] text-slate-400">{language}</p></div><Button type="button" size="sm" variant="ghost" className="text-slate-200 hover:bg-white/10 hover:text-white" onClick={()=>void navigator.clipboard.writeText(code).then(()=>{setCopied(true);setTimeout(()=>setCopied(false),1200)})}>{copied?<Check className="size-4"/>:<Copy className="size-4"/>}{copied?copiedLabel:copyLabel}</Button></div><pre className="overflow-x-auto p-5 text-sm leading-7"><code>{code}</code></pre>{explanation?<p className="border-t border-white/10 px-5 py-4 text-sm leading-6 text-slate-400">{explanation}</p>:null}</div>}
