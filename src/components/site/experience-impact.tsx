"use client";
import { useMemo, useState } from "react";
import { CheckCircle2 } from "lucide-react";
import { Card, CardContent } from "@/components/ui/card";
import { Button } from "@/components/ui/button";
type Experience={id:string;startDate:string;endDate:string|null;current:boolean;role:string;company:string;location:string;summary:string;highlights:string[];impactAreas?:string[]|null};
const canonical=["All","Financial Infrastructure","Platform Reliability","Engineering Leadership","Cloud & DevOps","Applied AI"];
export function ExperienceImpact({experiences}:{experiences:Experience[]}){const discovered=Array.from(new Set(experiences.flatMap(x=>x.impactAreas??[])));const areas=Array.from(new Set([...canonical,...discovered]));const [active,setActive]=useState("All");const rows=useMemo(()=>active==="All"?experiences:experiences.filter(x=>(x.impactAreas??[]).includes(active)),[active,experiences]);return <div><div className="mb-7 flex flex-wrap gap-2">{areas.map(area=><Button key={area} type="button" size="sm" variant={active===area?"default":"outline"} onClick={()=>setActive(area)}>{area}</Button>)}</div><div className="space-y-5">{rows.map(experience=><Card key={experience.id}><CardContent className="grid gap-6 p-6 lg:grid-cols-[260px_1fr] lg:p-8"><div><p className="text-sm font-bold text-primary">{experience.startDate} — {experience.current?"Present":experience.endDate}</p><h3 className="mt-2 text-xl font-bold">{experience.role}</h3><p className="mt-1 text-sm text-muted-foreground">{experience.company} · {experience.location}</p><div className="mt-3 flex flex-wrap gap-1">{(experience.impactAreas??[]).map(a=><span key={a} className="rounded-full bg-muted px-2 py-1 text-[10px] font-semibold text-muted-foreground">{a}</span>)}</div></div><div><p className="leading-7 text-muted-foreground">{experience.summary}</p><ul className="mt-5 grid gap-3">{(experience.highlights??[]).map(highlight=><li key={highlight} className="flex gap-3 text-sm leading-6 text-muted-foreground"><CheckCircle2 className="mt-1 size-4 shrink-0 text-primary"/>{highlight}</li>)}</ul></div></CardContent></Card>)}{!rows.length?<p className="rounded-xl border border-dashed border-border p-6 text-sm text-muted-foreground">No experience entries are tagged with this impact area yet.</p>:null}</div></div>}
